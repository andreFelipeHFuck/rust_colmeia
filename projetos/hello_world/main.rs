fn main(){ // Primeuro código executado em todos os programas Rust executávis.
    println!("Hello, world!");
    /*
    println! chama uma macro Rust
    Todas vez que usamos ! estamos chamando uma macro em vez de uma função normal.
    */
}